import React from "react";
import { View, Text } from "react-native";

const Card = data => {
  console.log(data);
  return (
    <View>
      <Text>card</Text>
    </View>
  );
};

export default Card;
